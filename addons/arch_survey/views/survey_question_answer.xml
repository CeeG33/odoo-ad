<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="survey_survey_question_form_inherit" model="ir.ui.view">
        <field name="name">Survey survey question form</field>
        <field name="model">survey.question</field>
        <field name="inherit_id" ref="survey.survey_question_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='suggested_answer_ids']/tree" position="inside">
                <field name="weight" column_invisible="not parent.apply_condition"/>
            </xpath>
            <xpath expr="//field[@name='matrix_row_ids']/.." position="after">
                <group>
                    <field
                        name="error_message"
                        string="Error message"
                        invisible="question_type != 'matrix' or not apply_condition"
                    />
                </group>
            </xpath>
            <xpath expr="//field[@name='matrix_row_ids']/.." position="before">
                <group>
                    <field name="apply_condition" invisible="question_type != 'matrix'"/>
                    <field name="should_be_equal" invisible="question_type != 'matrix' or not apply_condition"/>
                    <field
                        name="condition_to_meet"
                        string="Condition to meet"
                        invisible="question_type != 'matrix' or not apply_condition"
                    />
                </group>
            </xpath>
        </field>
    </record>
</odoo>
