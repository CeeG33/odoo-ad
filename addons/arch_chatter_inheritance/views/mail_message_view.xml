<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Used to hide/show messages depending on their is_hidden field -->
    <!-- <record id="view_message_list_inherit" model="ir.ui.view">
        <field name="name">mail.message.list.inherit</field>
        <field name="model">mail.message</field>
        <field name="inherit_id" ref="mail.mail_message_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@class='o_MessageList']/div[@t-foreach='messages']" position="attributes">
                <attribute name="t-if">not message.is_hidden</attribute>
            </xpath>
        </field>
    </record> -->

    <template id="mail_message_thread" inherit_id="mail.Chatter">
        <xpath expr="//div[@class='o_MessageList']/div[@t-foreach='messages']" position="attributes">
            <attribute name="t-if">not message.is_hidden</attribute>
        </xpath>
    </template>
    
</odoo>    
