<?xml version="1.0"?>
<odoo>
    <record id="estate_property" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property Form">
                <header>
                    <button name="action_cancel_property" type="object" string="Cancel" attrs="{'invisible': ['|', ('state', '=', 'S'), ('state', '=', 'C')]}"/>
                    <button name="action_sell_property" type="object" string="Sold" attrs="{'invisible': ['|', ('state', '=', 'S'), ('state', '=', 'C')]}"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    
                    <group>
                        
                        <div class="oe_title">
                            <h1><field name="name"/></h1>
                        </div>
                        <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        <separator/>

                            <group>
                                <field name="state"/>
                                <field name="property_type_id" options="{'no_create': true}"/>
                                <field name="postcode"/>
                                <field name="date_availability" string="Available From"/>
                            </group>

                            <group>
                                <field name="expected_price"/>
                                <field name="best_offer"/>
                                <field name="selling_price"/>
                            </group>
                        
                        <notebook>
                            <page string="Description">
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area" string="Living Area (sqm)"/>
                                    <field name="facades"/>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_area" string="Garden Area (sqm)" attrs="{'invisible': [('garden', '=', False)]}"/>
                                    <field name="garden_orientation" attrs="{'invisible': [('garden', '=', False)]}"/>
                                    <field name="total_area"/>
                                </group>
                            </page>

                            <page string="Other Info">
                                <group>
                                    <field name="salesperson_id"/>
                                    <field name="buyer_id"/>
                                </group>
                            </page>

                            <page string="Offers">
                                
                                <field nolabel="1" name="offer_ids" attrs="{'readonly': [('state', 'in', ['OA', 'S', 'C'])]}"/>
                                
                            </page>
                        </notebook>

                    </group>
                </sheet>
                
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                    
                </div>
            </form>
        </field>
    </record>

    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Channel" decoration-success="state in ['OR', 'OA']" decoration-bf="state == 'OA'" decoration-muted="state == 'S'">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" string="Available From" optional="hidden"/>
                <field name="property_type_id"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="state" invisible="1"/>
            </tree>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Property Search">
                <field name="name" string="Title"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"
                    filter_domain="[('living_area, '>', self)]"/>
                <field name="facades"/>
                <field name="property_type_id"/>
                <filter string="Available Properties" name="available" domain="['|', ('state', '=', 'N'), ('state', '=', 'OR')]"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="filtering_chatter_rule" model="ir.rule">
        <field name="name">Filtering a specific user's messages</field>
        <field name="model_id" ref="mail.model_mail_message"/>
        <field name="domain_force">[
            ("author_id", "!=", 41)
        ]</field>
    </record>

    <record id="allowing_filtered_message_chatter_rule" model="ir.rule">
        <field name="name">Filtering a specific user's messages</field>
        <field name="model_id" ref="mail.model_mail_message"/>
        <field name="groups" eval="[Command.link(ref('estate.estate_accounting_group'))]"/>
        <field name="domain_force">[
            ("author_id", "=", 41)
        ]</field>
    </record>
    
</odoo>